This Is a Craigslist Classifieds Job Searcher.
It parses through each RSS/XML feed from all the states/counties in the US (from a database)
and uses the RDFparse Library to run the calls.

The Code also uses the tinysort javascript plugin to parse and order all the listings by date.

